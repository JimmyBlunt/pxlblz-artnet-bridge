# v0.3 — Multi-Controller-Router: Bewertung, Plan, Umsetzung

**Branch:** `feature/v0.3-multi-controller` (rebased auf `main` @ `5c76e4b`, inkl. virtueller Integrationstests)
**Datum:** 2026-09-26
**Status:** in Software verifiziert (Unit-Tests + Loopback). **Hardware-Tests stehen noch aus** (V1–V4 unten).

---

## 1. Bewertung des Ausgangsstands (v0.2.0)

| Bereich | Stand | Bewertung |
|---|---|---|
| Router-Kern (ArtDmx, Routing, Farbreihenfolge) | fertig, auf Hardware getestet | ✅ klein, sauber, getestet |
| Receiver-Kompatibilität (gerade Payload, gemeinsame Sequenz) | fertig (H4) | ✅ |
| WebSocket-Input + LatestFrame | fertig (H6/H7) | ✅ keine Latenz-Queue |
| PXLBLZ-Adapter `?pxout=1` | Transport ok | 🟡 experimentell |
| Visuelle Pattern-Verifikation (G9) | offen | 🔴 blockiert durch Windows-Studio-Login |
| .244 / .251 / APA102 (G10) | nur dokumentiert | 🔴 |
| Multi-Controller-Scheduling (G12) | fehlt | 🔴 Lücke in der Architektur |
| Verhalten bei Input-Abbruch | fehlt | 🔴 |
| Telemetrie | nur Konsole | 🟡 |

**Entscheidung:** Kein Neubau. Der v0.2-Kern ist auf Hardware verifiziert und bleibt Byte für Byte gleich
(ein Test sichert das ab). v0.3 ergänzt nur die fehlende Controller-Ebene darüber.

Gefundene Schwachstellen in v0.2:

1. Eine **globale** Sequenznummer und **ein** Takt für alle Controller. Für .244 (60 FPS) + .251/.253 (30 FPS) passt das nicht.
2. Stoppt PXLBLZ, wird das letzte Bild endlos weitergesendet – ohne Timeout und ohne Blackout.
3. Keine Tests für `config`, keine Warnung bei doppelt gelesenen logischen Pixeln.
4. Der Parallel-Branch `feature/fadecandy-l3d-output` (22 Commits: Fadecandy/L3D, Windows-Launcher) ist nicht in `main`. Er wird hier nicht angefasst.

---

## 2. Was v0.3 ändert

```text
                 ┌── WS input (PXLBLZ) ──┐   oder   ┌── Pattern-Generator ──┐
                 └───────────┬───────────┘          └───────────┬───────────┘
                             ▼                                  ▼
                      LatestFrame (ein Slot, keine Queue)
                             │
      ┌──────────────────────┼──────────────────────┐
      ▼                      ▼                      ▼
 WS2812_NODE_244        PANEL8_251            BACK_PANEL_249 (.253)
 eigene Goroutine       eigene Goroutine      eigene Goroutine
 60 fps, eigene Seq     30 fps, eigene Seq    30 fps, eigene Seq
 1 Snapshot → Burst     1 Snapshot → Burst    1 Snapshot → 29 Universen
```

### 2.1 Config v2 (v1 bleibt gültig)

```json
{
  "version": 2,
  "input": {"pixel_count": 8186, "fps_target": 60,
            "stale_timeout_ms": 2000, "on_stale": "hold"},
  "controllers": [
    {"name": "WS2812_NODE_244", "target_ip": "10.0.0.244", "fps_target": 60},
    {"name": "BACK_PANEL_249",  "target_ip": "10.0.0.253", "fps_target": 30, "enabled": true}
  ],
  "routes": [ … wie bisher … ]
}
```

- `controllers[]` ist optional. Fehlt es, wird pro Ziel-IP automatisch ein Controller mit `input.fps_target` angelegt. Damit verhalten sich alle v1-Configs wie bisher.
- Wenn `controllers[]` gesetzt ist, muss jede aktive Route eine passende `target_ip` haben.
- `enabled: false` auf einem Controller schaltet alle seine Routen ab.
- `stale_timeout_ms` / `on_stale`:
  - `hold` (Standard) sendet das letzte Bild weiter (Verhalten wie v0.2),
  - `blackout` sendet Schwarz,
  - `stop` sendet nichts, bis wieder Input kommt.
- Vor dem **ersten** gültigen Frame wird nie gesendet (wie in v0.2).
- Beim Start gibt es Warnungen, wenn sich die logischen Pixel zweier Routen überlappen oder ein Controller keine Routen hat.

### 2.2 Controller-Ebene im Router

- `Controller` besitzt Sequenz, Paketpuffer und Statistik. Die Sequenz wird erst erhöht, wenn der **ganze Controller-Frame** gesendet ist, und springt beim Überlauf von 255 auf 1, nie auf 0.
- `SendFrame()` (alle Controller) funktioniert unverändert. `SendController()` ist neu.

### 2.3 Scheduler (`internal/scheduler`)

- Jeder Controller läuft mit eigenem Ticker. Pro Tick nimmt er **einen** Snapshot aus dem LatestFrame und sendet daraus alle seine Universen.
- Controller warten nicht aufeinander und bauen keine Queue auf.
- `--fps N` setzt alle Controller auf N (für Tests).

### 2.4 Pattern `panel-walk` (für G9, ohne PXLBLZ)

- Alle Routen leuchten schwach in ihrer Port-Farbe: P1 rot, P2 grün, P3 blau, P4 cyan, P5 magenta, P6 gelb, P7 weiß.
- Die ersten 3 Pixel jeder Lane sind heller. Sie markieren den elektrischen **Anfang** und damit die Richtung.
- Ein weißer Kopf mit farbigem Schweif läuft in Config-Reihenfolge durch alle Routen (P1 → … → P7).
- Die Helligkeit ist bewusst niedrig: Hintergrund 6/255, Kopf 160/255.
- `--walk-speed` steuert die Geschwindigkeit (Pixel pro Frame, Standard 4 → ca. 34 s pro Runde auf BACK_PANEL).

### 2.5 Status-Endpunkt

`GET http://127.0.0.1:9981/status` liefert JSON mit RX-Zählern, dem Alter des letzten Frames, WS-Clients und
pro Controller Frames, Pakete, Fehler, Wiederholungen, Stale-Flag, Sendezeiten und die erwartete Universen-Liste.
CORS ist offen, damit eine spätere PXLBLZ-Output-UI (G11) die Daten direkt lesen kann. Mit `--status-listen ""` wird der Endpunkt abgeschaltet.

### 2.6 Neue Dateien

| Datei | Zweck |
|---|---|
| `router/internal/scheduler/` | Scheduler pro Controller + Tests |
| `router/internal/config/config_test.go` | Config-Tests; alle ausgelieferten Configs müssen laden |
| `router/internal/router/multi_test.go` | sichert BACK_PANEL-Frame, Sequenzen und Wrap ab |
| `router/config/routes.installation-full.json` | .244 (GRB, 60), .251 (BGR, 30), .253 (RGB, 30) |
| `router/config/routes.multi-loopback.json` | gleiche Topologie auf 127.0.0.1/.2/.3 |
| `router/run-backpanel-panel-walk-test.bat` | G9-Sichttest |
| `router/run-multi-loopback-test.bat` | Loopback mit 3 Controllern + Stale-Test |
| `router/run-installation-ws.bat` | ganze Installation, Live-WS-Input |

Neue CLI-Flags: `--pattern panel-walk`, `--walk-speed`, `--on-stale`, `--stale-timeout-ms`,
`--status-listen`, `--per-controller-stats`.

---

## 3. Verifikation in diesem Branch

- `go vet ./...`, `go test -race ./...`: alles grün (siehe `router/TEST_RESULTS.md`, **L1**)
- Loopback mit 3 Controllern: 60/30/30 fps, 1590 Pakete/s, 0 ungültige Pakete, 0 Sendefehler, Blackout nach 1 s ohne Input
- Virtueller E2E-Test aus `main` (`node pxlblz-integration/virtual-test/run-virtual-e2e.mjs`) läuft auch mit v0.3 grün: `ALL_VIRTUAL_TESTS_PASS`
- Windows-x64-EXEs neu gebaut unter `bin/windows-x64/` (Prüfsummen in `SHA256SUMS.txt`)

**Nicht verifiziert:** echte Hardware.

---

## 4. Hardware-Testplan (in dieser Reihenfolge)

| # | Test | Befehl | PASS-Kriterium |
|---|---|---|---|
| **V1** | Regression BACK_PANEL mit v0.3 | `run-backpanel-port-id-test.bat` | wie H5: DMA ~30/s, `incomplete` steigt nicht, 7 Lanes in richtiger Farbe |
| **V2** | G9 Sichttest ohne PXLBLZ | `run-backpanel-panel-walk-test.bat` | Kopf läuft P1→P7, Startmarker am erwarteten Panel-Ende, Übergang P6 (gelb) → P7 (weiß) ohne Lücke |
| **V3** | Stale-Verhalten | `pxlblz-router.exe --config config\routes.backpanel-all.json --input ws --on-stale blackout --stale-timeout-ms 1000` + Sender stoppen | Panels werden nach ~1 s schwarz, beim Neustart des Senders geht es ohne Router-Neustart weiter |
| **V4** | G10 .244 / .251 | `run-installation-ws.bat` + `pxlblz-frame-sender.exe --config config\routes.installation-full.json --pattern rainbow` | jeder Controller zeigt Bild; bei .244 GRB und bei .251 BGR stimmen die Farben (reine Rot/Grün/Blau-Tests) |

Danach: V2 noch einmal mit echtem PXLBLZ-Pattern über `?pxout=1` (das ursprüngliche G9), sobald der Studio-Login unter Windows läuft.

Ergebnisse bitte als **H8…** in `router/TEST_RESULTS.md` eintragen.

---

## 5. Nächste Schritte nach v0.3

1. **V1–V4 auf Hardware**, danach Branch nach `main` mergen und als v0.3.0 taggen (die CI baut auf `main` automatisch die EXEs).
2. **Windows-Studio-Login** (G8) nach `docs/WINDOWS_PXLBLZ_LOCAL_STUDIO.md`. Das ist unabhängig vom Router.
3. **APA102-Route** bestätigen und in `routes.installation-full.json` eintragen.
4. **G11 Output-UI in PXLBLZ**: liest `/status`, schaltet Output an/aus, wählt die URL und ersetzt `?pxout=1`.
5. Fadecandy-Branch prüfen: was davon (Launcher, variable Frame-Größe) nach `main` soll.
6. Backlog: Helligkeit pro Controller, Paket-Pacing nur bei Bedarf, End-to-End-Latenz-Telemetrie, ArtPoll, DDP/sACN.

---

## 6. Entkopplung von parallel laufender Arbeit auf `main` (ab 2026-09-26)

Auf `main` wird parallel weiterentwickelt (u. a. virtueller Teensy-Controller,
Receiver-Probe, Änderungen in `router.go` / `config.go`). Damit sich v0.3 und diese
Arbeit nicht gegenseitig stören, gilt bis zum Abschluss von V1–V4:

1. **Feste Basis.** v0.3 zieht `main` **nicht** mehr nach. Basis ist `main@04eba84`
   plus die bereits übernommenen Lab-Fixes. Ein einziger Integrations-Merge mit `main`
   folgt erst, wenn V1–V4 bestanden sind; danach laufen die Software-Tests einmal neu.
2. **Keine EXEs im Branch.** `bin/windows-x64/` wird auf diesem Branch nicht mehr
   aktualisiert (die CI auf `main` baut und committet die EXEs selbst — jeder Commit dort
   erzeugt sonst einen sicheren Binär-Konflikt). Test-EXEs für die Hardware werden
   außerhalb des Repos gebaut: `PXLBLZ_ArtNet_Handoff_2026-09-26\v0.3-test-bin\`
   (mit `SHA256SUMS.txt` und `SOURCE_COMMIT.txt`). Beim Integrations-Merge gewinnt
   `bin/` von `main`, die CI baut danach neu.
3. **Tests gegen den eigenen Stand.** Soak- und Reservetests messen nur v0.3.
   Lokale Test-Ports des Performance-Labs sind über `--udp-port` / `--ws-port`
   verschiebbar, falls auf demselben PC parallel getestet wird.
4. **Umgekehrt** (Empfehlung für die parallele Arbeit): eigene Feature-Branches statt
   direkter Pushes auf `main`; Änderungen am Router-Kern (`internal/router`,
   `internal/config`, `cmd/pxlblz-router`) möglichst erst nach dem v0.3-Merge.
